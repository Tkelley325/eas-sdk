name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  test:
    name: Test

    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v5

      - name: Install PNPM
        uses: pnpm/action-setup@v6
        id: pnpm-install
        with:
          version: 9
          run_install: false

      - name: Install Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install SDK dependencies
        run: pnpm install

      - name: Install SDK test dependencies
        run: cd test && pnpm install --ignore-workspace

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test
